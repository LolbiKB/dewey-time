// Re-export shim: this primitive now lives in the shared Dewey design
// system. Keep importing from "@/components/ui/empty-state" — only the source moved.
export { EmptyState } from "@lolbikb/dewey-ui"
