# Copy to `.env.local` for `npm run dev` (gitignored if you add .env.local locally).
#
# Frappe Cloud — live data, no local bench:
FRAPPE_PROXY=https://dewey.frappehr.com
#
# Local bench (only if you run `bench serve` on :8000):
# FRAPPE_PROXY=http://127.0.0.1:8000
