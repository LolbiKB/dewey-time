
# Superpowers brainstorm scratch (visual companion)
.superpowers/

# Frontend deps are never committed (Frappe Cloud ships pre-built public/ bundles)
dewey_time/frontend/*/node_modules/
**/__pycache__/
*.egg-info/
.DS_Store
.claude/
.playwright-mcp/

# Employee schedule data + conversion outputs (PII — never commit)
dewey_schedule_*.csv
dewey_schedule_*.md
*.xlsx - *.csv

# Pre-rollout audit screenshots (local artifacts, never committed)
dewey_time/frontend/hr_attendance/e2e/.audit-shots/

# Stray design/QA screenshots dropped at the repo root (local scratch, never
# committed). Root-scoped so committed PNGs under public/, images/, etc. are
# unaffected.
/*.png
