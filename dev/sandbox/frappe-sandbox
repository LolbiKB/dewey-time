#!/usr/bin/env bash
# Thin shim — location-independent so it works from the repo root or dev/sandbox/.
here="$(cd "$(dirname "$0")" && pwd)"
exec env PYTHONPATH="$here${PYTHONPATH:+:$PYTHONPATH}" python3 -m frappe_sandbox.cli "$@"
